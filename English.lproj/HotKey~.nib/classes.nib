{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancel = id; ok = id; }; 
            CLASS = HotKeyController; 
            LANGUAGE = ObjC; 
            OUTLETS = {keysField = NSTextField; mainWindow = NSWindow; typeField = NSTextField; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = HotKeyTextField; LANGUAGE = ObjC; SUPERCLASS = NSTextField; }
    ); 
    IBVersion = 1; 
}